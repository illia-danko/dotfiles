# -----------------------------------------------------------------------------
# User configuration
[ -f "$HOME/.appearance.sh" ] && . "$HOME/.appearance.sh"
[ -f "$HOME/.config-common.sh" ] && . "$HOME/.config-common.sh"
[ -f "$HOME/.config-work.sh" ] && . "$HOME/.config-work.sh"

# [ -z $DISPLAY ] && [ "$(tty)" = "/dev/tty1" ] && exec sway
