auto-reload yes
reload-threads 100
text-width 80

# keybindings
unbind-key j
unbind-key k
unbind-key h
unbind-key l
unbind-key c

bind-key j down
bind-key k up
bind-key h quit
bind-key l open
bind-key c toggle-show-read-feeds

browser "xdg-open-silently"
